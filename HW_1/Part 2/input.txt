   name() {
        "John"
    }

    repeat(x) {
        x + x
    }

    cond_repeat(c, x) {
        if (c prefix "yes")
            if("yes" prefix c)
                repeat(x,x, "yeet")
            else
                x
        else
            x
    }

    cond_repeat("yes", name())
    cond_repeat("no", "Jane", if("yes" prefix "yes") func() else "yeet")